@model IReadOnlyList<FishingBuddy.Models.Technique>
@{
    var requestMode = Context.Request.Query["mode"].ToString();
    var currentMode = string.Equals(requestMode, "delete", StringComparison.OrdinalIgnoreCase) ? "delete" : "edit";
    ViewData["Title"] = "Upravljanje tehnikama";
    ViewData["BreadcrumbItems"] = new List<(string, string?)>
    {
        ("Početna", "/"),
        ("Tehnike", "/Technique"),
        ("Upravljanje", null)
    };
}

<section class="card shadow-sm border-0">
    <div class="card-body">
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-3 fb-page-header">
            <div class="fb-page-mobile-titlebar d-md-none">
                <h1 class="h2 mb-0">Upravljanje tehnikama</h1>
                <div class="dropdown fb-crud-dropdown">
                    <button class="btn btn-outline-dark dropdown-toggle fb-crud-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">CRUD</button>
                    <ul class="dropdown-menu fb-crud-menu">
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--create" asp-controller="Technique" asp-action="Create"><span class="badge bg-success rounded-pill me-2">Create</span></a></li>
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--update" asp-controller="Technique" asp-action="Manage" asp-route-mode="edit"><span class="badge bg-warning text-dark rounded-pill me-2">Update</span></a></li>
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--delete" asp-controller="Technique" asp-action="Manage" asp-route-mode="delete"><span class="badge bg-danger rounded-pill me-2">Delete</span></a></li>
                    </ul>
                </div>
            </div>
            <p class="text-muted mb-0 d-md-none fb-page-mobile-subtitle">Administrirajte tehnike, poveznice na vodice i povezane vrste riba.</p>
            <div class="d-none d-md-block">
                <h1 class="h2 mb-1">Upravljanje tehnikama</h1>
                <p class="text-muted mb-0">Administrirajte tehnike, poveznice na vodice i povezane vrste riba.</p>
            </div>
            <div class="d-flex align-items-center gap-2 fb-list-toolbar-actions">
                <span class="badge bg-primary">@Model.Count tehnika</span>
                <div class="dropdown fb-crud-dropdown d-none d-md-block">
                    <button class="btn btn-outline-dark dropdown-toggle fb-crud-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">CRUD</button>
                    <ul class="dropdown-menu fb-crud-menu">
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--create" asp-controller="Technique" asp-action="Create"><span class="badge bg-success rounded-pill me-2">Create</span></a></li>
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--update" asp-controller="Technique" asp-action="Manage" asp-route-mode="edit"><span class="badge bg-warning text-dark rounded-pill me-2">Update</span></a></li>
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--delete" asp-controller="Technique" asp-action="Manage" asp-route-mode="delete"><span class="badge bg-danger rounded-pill me-2">Delete</span></a></li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="fb-list-search"
             data-ajax-search
             data-search-endpoint="@Url.Action("Search", "Technique")"
             data-search-mode="@currentMode"
             data-empty-colspan="4"
             data-empty-message="Nema trazenih tehnika.">
            <label class="form-label" for="techniqueManageSearch">Pretrazi tehnike</label>
            <input id="techniqueManageSearch" type="search" class="form-control" placeholder="Naziv, tutorial ili povezane ribe..." data-search-input autocomplete="off" />
            <div class="form-text fb-search-status" data-search-status aria-live="polite">Prikaz svih zapisa.</div>

            <div class="table-responsive">
            <table class="table table-hover table-bordered table-sm align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th scope="col">Naziv</th>
                        <th scope="col">Tutorial</th>
                        <th scope="col" class="text-center">Povezane ribe</th>
                        <th scope="col" class="text-end">Radnje</th>
                    </tr>
                </thead>
                <tbody data-search-target>
                    @await Html.PartialAsync("_TechniqueRows", Model, new ViewDataDictionary(ViewData) { ["ManageMode"] = true, ["CrudMode"] = currentMode })
                </tbody>
            </table>
        </div>
        </div>
    </div>
</section>

