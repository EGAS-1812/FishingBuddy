@model IReadOnlyList<FishingBuddy.Models.Fish>
@{
    ViewData["Title"] = "Upravljanje vrstama riba";
    ViewData["BreadcrumbItems"] = new List<(string, string?)>
    {
        ("Početna", "/"),
        ("Vrste riba", "/Fish"),
        ("Upravljanje", null)
    };
}

<section class="card shadow-sm border-0">
    <div class="card-body">
        <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-3">
            <div>
                <h1 class="h2 mb-1">Upravljanje vrstama riba</h1>
                <p class="text-muted mb-0">Administrirajte vrste riba, sezone lova i preferiranu opremu.</p>
            </div>
            <div class="d-flex align-items-center gap-2">
                <span class="badge bg-primary">@Model.Count vrsta</span>
                <div class="dropdown fb-crud-dropdown">
                    <button class="btn btn-outline-dark dropdown-toggle fb-crud-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">CRUD</button>
                    <ul class="dropdown-menu fb-crud-menu">
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--create" asp-controller="Fish" asp-action="Create"><span class="badge bg-success rounded-pill me-2">Create</span></a></li>
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--update" asp-controller="Fish" asp-action="Manage"><span class="badge bg-warning text-dark rounded-pill me-2">Update</span></a></li>
                        <li><a class="dropdown-item fb-crud-item fb-crud-item--delete" asp-controller="Fish" asp-action="Manage"><span class="badge bg-danger rounded-pill me-2">Delete</span></a></li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="fb-list-search"
             data-ajax-search
             data-search-endpoint="@Url.Action("Search", "Fish")"
             data-search-mode="manage"
             data-empty-colspan="4"
             data-empty-message="Nema trazenih vrsta riba.">
            <label class="form-label" for="fishManageSearch">Pretrazi vrste riba</label>
            <input id="fishManageSearch" type="search" class="form-control" placeholder="Vrsta, sezona ili boja mesa..." data-search-input autocomplete="off" />
            <div class="form-text fb-search-status" data-search-status aria-live="polite">Prikaz svih zapisa.</div>

            <div class="table-responsive">
            <table class="table table-hover table-bordered table-sm align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th scope="col">Naziv vrste</th>
                        <th scope="col">Sezona lova</th>
                        <th scope="col">Boja mesa</th>
                        <th scope="col" class="text-end">Radnje</th>
                    </tr>
                </thead>
                <tbody data-search-target>
                    @await Html.PartialAsync("_FishRows", Model, new ViewDataDictionary(ViewData) { ["ManageMode"] = true })
                </tbody>
            </table>
        </div>
        </div>
    </div>
</section>


